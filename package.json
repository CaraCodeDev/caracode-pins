{
  "name": "caracode-pins-workspace",
  "private": true,
  "type": "module",
  "packageManager": "pnpm@12.5.1",
  "engines": {
    "node": ">=22.12.0"
  },
  "scripts": {
    "dev": "pnpm --filter @caracode/pins build && concurrently --kill-others-on-fail --names pins,playground --prefix-colors cyan,magenta \"pnpm --filter @caracode/pins dev\" \"pnpm --filter playground dev\"",
    "build": "pnpm -r build",
    "typecheck": "pnpm -r typecheck"
  },
  "devDependencies": {
    "concurrently": "^10.0.5",
    "typescript": "^7.0.2"
  }
}
